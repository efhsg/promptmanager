# Todos — export-content-locally

- [x] Add Download radio button between Clipboard and File in HTML
- [x] Split `export-file-options` div into `export-filename-options` and `export-directory-options`
- [x] Replace `EXTENSION_MAP` with `FORMAT_MAP` (includes mime types)
- [x] Update `getElements()` with new element references
- [x] Refactor `toggleFileOptions()` → `toggleDestinationOptions()` for 3 states
- [x] Implement `exportToDownload()` function (Blob + createObjectURL)
- [x] Refactor `handleExport()` to switch on 3 destination values
- [x] Update `open()` for new Download radio button reset
- [x] Update `init()` for new event listener on Download radio
- [x] Run linter + tests — 0 issues, 985 tests pass
